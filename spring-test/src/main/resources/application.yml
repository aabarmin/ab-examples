spring:
  datasource:
    url: jdbc:h2:file:./build/demo-db
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: '*'